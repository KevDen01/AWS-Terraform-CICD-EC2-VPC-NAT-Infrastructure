name: Terraform CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  terraform:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: src

    steps:
      # Step 1: Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v3

      # Step 2: Configure AWS Credentials
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: us-west-2

      # Step 3: Create S3 Bucket for Terraform State
      - name: Create S3 Bucket for Terraform State
        run: |
          BUCKET_NAME=my-terraform-state-bucket-${{ github.run_id }}

          # Check if the bucket exists
          if aws s3api head-bucket --bucket $BUCKET_NAME 2>/dev/null; then
            echo "S3 bucket $BUCKET_NAME already exists. Skipping creation."
          else
            echo "Creating S3 bucket: $BUCKET_NAME..."
            aws s3api create-bucket --bucket $BUCKET_NAME --region us-west-2 --create-bucket-configuration LocationConstraint=us-west-2
          fi

          # Export the bucket name for later steps
          echo "bucket_name=$BUCKET_NAME" >> $GITHUB_ENV


      # Step 4: Create DynamoDB Table for State Locking
      - name: Create DynamoDB Table for State Locking
        run: |
          TABLE_NAME=my-terraform-lock-table-${{ github.run_id }}

          # Check if the table exists
          if aws dynamodb describe-table --table-name $TABLE_NAME 2>/dev/null; then
            echo "DynamoDB table $TABLE_NAME already exists. Skipping creation."
          else
            echo "Creating DynamoDB table: $TABLE_NAME..."
            aws dynamodb create-table \
              --table-name $TABLE_NAME \
              --attribute-definitions AttributeName=LockID,AttributeType=S \
              --key-schema AttributeName=LockID,KeyType=HASH \
              --billing-mode PAY_PER_REQUEST
          fi

          # Export the table name for later steps
          echo "dynamodb_table=$TABLE_NAME" >> $GITHUB_ENV


      # Step 5: Create EC2 Key Pairs
      - name: Create EC2 Key Pair
        run: |
          # Use the GitHub run ID for unique naming
          RUN_ID=${{ github.run_id }}

          # Private key
          PRIVATE_KEY_NAME="key-ec2-private-$RUN_ID"
          if aws ec2 describe-key-pairs --key-names $PRIVATE_KEY_NAME 2>/dev/null; then
            echo "Key pair $PRIVATE_KEY_NAME already exists. Skipping creation."
          else
            echo "Creating private key pair: $PRIVATE_KEY_NAME..."
            aws ec2 create-key-pair --key-name $PRIVATE_KEY_NAME --query 'KeyMaterial' --output text > $PRIVATE_KEY_NAME.pem
            chmod 400 $PRIVATE_KEY_NAME.pem
          fi

          # Public key
          PUBLIC_KEY_NAME="key-ec2-public-$RUN_ID"
          if aws ec2 describe-key-pairs --key-names $PUBLIC_KEY_NAME 2>/dev/null; then
            echo "Key pair $PUBLIC_KEY_NAME already exists. Skipping creation."
          else
            echo "Creating public key pair: $PUBLIC_KEY_NAME..."
            aws ec2 create-key-pair --key-name $PUBLIC_KEY_NAME --query 'KeyMaterial' --output text > $PUBLIC_KEY_NAME.pem
            chmod 400 $PUBLIC_KEY_NAME.pem
          fi

          # Export the key pair names for later steps
          echo "private_key_name=$PRIVATE_KEY_NAME.pem" >> $GITHUB_ENV
          echo "public_key_name=$PUBLIC_KEY_NAME.pem" >> $GITHUB_ENV


      # Step 6: Update run_id in terraform.tfvars
      - name: Update run_id in terraform.tfvars
        run: |
          RUN_ID=${{ github.run_id }}
          # Check if the file exists and already has run_id
          if grep -q "^run_id" terraform.tfvars; then
            # Replace the run_id line
            sed -i '' "s/^run_id.*/run_id = \"$RUN_ID\"/" terraform.tfvars
          else
            # Append the run_id line if it doesn't exist
            echo "run_id = \"$RUN_ID\"" >> terraform.tfvars
          fi
          echo "Terraform variable 'run_id' set to '$RUN_ID'"


      # Step 7: Generate Updated Backend
      - name: Generate Backend Configuration
        run: |
          cat <<EOF > backend.tf
          terraform {
            backend "s3" {
              bucket         = "$bucket_name"
              key            = "terraform/state.tfstate"
              region         = "us-west-2"
              encrypt        = true
              dynamodb_table = "$dynamodb_table"
              acl            = "bucket-owner-full-control"
            }
          }
          EOF

      # Step 8: Set up Terraform
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.5.0

      # Step 9: Initialize Terraform
      - name: Terraform Init
        run: terraform init

      # Step 10: Terraform Plan
      - name: Terraform Plan
        run: terraform plan

      # Step 11: Terraform Apply
      - name: Terraform Apply
        if: github.event_name == 'push'
        run: terraform apply -auto-approve

      # # Step 12: Terraform Destroy
      # - name: Terraform Destroy
      #   run: terraform destroy --auto-approve

      # # Step 13: Empty and Delete S3 Bucket
      # - name: Empty and Delete S3 Bucket
      #   run: |
      #     BUCKET_NAME=my-terraform-state-bucket-${{ github.run_id }}
      #     echo "Emptying S3 bucket: $BUCKET_NAME..."
      #     aws s3 rm s3://$BUCKET_NAME --recursive
      #     echo "Deleting S3 bucket: $BUCKET_NAME..."
      #     aws s3api delete-bucket --bucket $BUCKET_NAME --region us-west-2

      # # Step 14: Delete DynamoDB Table
      # - name: Delete DynamoDB Table
      #   run: |
      #     TABLE_NAME=my-terraform-lock-table-${{ github.run_id }}
      #     echo "Deleting DynamoDB table: $TABLE_NAME..."
      #     aws dynamodb delete-table --table-name $TABLE_NAME --region us-west-2

      # # Step 15: Delete EC2 Key Pairs
      # - name: Delete EC2 Key Pairs
      #   run: |
      #     RUN_ID=${{ github.run_id }}
      #     echo "Deleting key pair: key-ec2-private-$RUN_ID..."
      #     aws ec2 delete-key-pair --key-name key-ec2-private-$RUN_ID
      #     echo "Deleting key pair: key-ec2-public-$RUN_ID..."
      #     aws ec2 delete-key-pair --key-name key-ec2-public-$RUN_ID

